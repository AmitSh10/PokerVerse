[package]
name = "poker_server"
version = "0.1.0"
edition = "2024"

[dependencies]
poker_engine = { path = "../poker_engine" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
